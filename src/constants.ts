export enum FPTI_CONTEXT_TYPE {
  ORDER_ID = "EC-Token",
}
export enum FPTI_TRANSITION {
  CREATE_ORDER = "process_create_order",
  SCRIPT_LOAD = "process_script_load",
  PXP = "process_pxp_check",
}
export enum FPTI_STATE {
  PXP = "PXP_CHECK",
}
